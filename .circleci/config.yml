version: 2.1

orbs:
  codecov: codecov/codecov@4.0.1

jobs:
  build-and-test:
    docker:
      - image: cimg/openjdk:11.0  
    steps:
      - checkout
      - run:
          name: Build
          command: mvn -B -DskipTests clean package 
      - run:
          name: Unit Tests
          command: mvn test  
      - run:
          name: Checkstyle
          command: mvn checkstyle:check  
      - run:
          name: Generate Coverage Report
          command: mvn jacoco:report
      - codecov/upload:
          file: target/site/jacoco/jacoco.xml
      
      - run:
          name: Générer les Javadocs
          command: mvn javadoc:javadoc
      - run:
          name: Déployer les Javadocs sur GitHub Pages
          command: |
            git config --global user.name "circleci"
            git config --global user.email "circleci@example.com"
            git clone --branch gh-pages https://github.com/<votre-utilisateur>/<nom-de-votre-depot>.git gh-pages
            cp -r target/site/apidocs/* gh-pages/docs/
            cd gh-pages
            git add docs/
            git commit -m "Mise à jour des Javadocs"
            git push origin gh-pages
      
      

workflows:
  version: 2
  master-branch-workflow:
    jobs:
      - build-and-test:
          filters:
            branches:
              only: master